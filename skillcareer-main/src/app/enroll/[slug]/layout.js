import React from "react"

export const metadata = {
  title: "Enroll | SkillCareer",
  description: "Enroll in SkillCareer courses. Get fee details, batch timings, and a counselor callback.",
}

export default function EnrollCourseLayout({ children }) {
  return <main>{children}</main>
}

